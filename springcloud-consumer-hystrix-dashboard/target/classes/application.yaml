server:
  port: 9001
#暴露全部的监控信息
  hystrix:
    dashboard:
      proxy-stream-allow-list: "*"
      #暴露全部的监控信息
      management:
        endpoints:
          web:
            exposure:
              include: "*"
